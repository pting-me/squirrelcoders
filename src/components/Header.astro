---
import Logo from './Logo.astro';
import Nav from './Nav.astro';

const { current = '' } = Astro.props;
---

<header class="flex my-0 mx-auto max-w-7xl p-8 w-full">
  <Logo />
  <Nav current={current} />
</header>
